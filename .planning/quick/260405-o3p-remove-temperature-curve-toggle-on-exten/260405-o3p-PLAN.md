---
phase: quick-260405-o3p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brewos-engine/keif-mobile/components/TempCurveInline.tsx
autonomous: true
requirements:
  - QUICK-260405-O3P-01
must_haves:
  truths:
    - "Temperature curve chart is always visible in the Temperature at End card when temperature_curve data is present"
    - "No 'View curve' / 'Hide curve' toggle button is rendered anywhere in the Extended Output screen"
    - "Cards without temperature_curve data remain unchanged (no chart)"
  artifacts:
    - path: "brewos-engine/keif-mobile/components/TempCurveInline.tsx"
      provides: "Unconditionally-rendered temperature decay chart"
      contains: "CartesianChart"
  key_links:
    - from: "brewos-engine/keif-mobile/app/extended.tsx"
      to: "brewos-engine/keif-mobile/components/TempCurveInline.tsx"
      via: "<TempCurveInline data={currentOutput.temperature_curve} />"
      pattern: "TempCurveInline"
---

<objective>
Remove the "View curve" / "Hide curve" toggle from the Temperature at End card on the Extended Output screen. The temperature decay chart must always render when temperature_curve data is present.

Purpose: Simplify the Extended Output UX — the chart was already conditionally rendered at the parent level (only when temperature_curve has data), so the additional in-card toggle is redundant friction.

Output: Updated TempCurveInline.tsx with toggle removed and chart rendered unconditionally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Current TempCurveInline.tsx contract (to be simplified) -->

From brewos-engine/keif-mobile/components/TempCurveInline.tsx:
```typescript
interface TempCurveInlineProps {
  data: TempPoint[];
}
export function TempCurveInline({ data }: TempCurveInlineProps): JSX.Element;
```

From brewos-engine/keif-mobile/types/simulation.ts:
```typescript
// TempPoint has shape: { t: number; temp_c: number }
```

Parent usage in brewos-engine/keif-mobile/app/extended.tsx (lines 104-113):
```tsx
{currentOutput.temperature_curve !== null &&
  currentOutput.temperature_curve.length > 0 && (
    <ExtendedDetailCard
      label="Temperature at End"
      value={`${currentOutput.temperature_curve[currentOutput.temperature_curve.length - 1].temp_c.toFixed(1)}\u00B0C`}
    >
      <TempCurveInline data={currentOutput.temperature_curve} />
    </ExtendedDetailCard>
  )}
```

Note: Parent already guards on `temperature_curve !== null && length > 0`, so TempCurveInline can assume `data` is non-empty.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove toggle state and render chart unconditionally in TempCurveInline</name>
  <files>brewos-engine/keif-mobile/components/TempCurveInline.tsx</files>
  <action>
    Simplify TempCurveInline.tsx to always render the CartesianChart when called:

    1. Remove `useState` import from React (no longer needed) — keep `React` default import.
    2. Remove `TouchableOpacity` and `Text` from the react-native import line.
    3. Delete the `const [expanded, setExpanded] = useState(false);` line.
    4. Delete the `<TouchableOpacity onPress={...}><Text style={styles.toggle}>...</Text></TouchableOpacity>` block entirely.
    5. Remove the `{expanded && (...)}` conditional wrapper around the chart View — render the chart View unconditionally.
    6. Delete the `styles` StyleSheet (only contained the now-unused `toggle` style) and remove the `StyleSheet` import.

    The resulting component should:
    - Compute chartWidth/chartHeight and domain bounds (maxT, maxTemp, minTemp) as before.
    - Return a single `<View style={{ height: chartHeight, marginTop: 8 }}>` containing the CartesianChart with the existing Line rendering logic unchanged.
    - Keep the same `TempCurveInlineProps` interface (still accepts `data: TempPoint[]`).
    - Keep all existing type casts (`as unknown as Record<string, unknown>[]`, `as never`, `any` for points) — these are required by Victory Native per the phase 07 decision logged in STATE.md.

    Do NOT touch extended.tsx — the parent-level guard on `temperature_curve !== null && length > 0` already handles the "no data" case.
  </action>
  <verify>
    <automated>cd brewos-engine/keif-mobile && npx tsc --noEmit</automated>
  </verify>
  <done>
    - TempCurveInline.tsx has no `useState`, no `TouchableOpacity`, no `Text`, no `expanded` state, no `styles`/`StyleSheet`.
    - Chart View is rendered unconditionally inside the component's return.
    - `npx tsc --noEmit` passes with zero errors.
    - Running the Extended Output screen shows the temperature chart inline without any "View curve" button when temperature_curve data exists.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles cleanly: `cd brewos-engine/keif-mobile && npx tsc --noEmit`
2. Grep confirms toggle text is gone: `grep -r "View curve\|Hide curve" brewos-engine/keif-mobile/` returns no matches.
3. Grep confirms no orphan imports: TempCurveInline.tsx should not import `useState`, `TouchableOpacity`, `Text`, or `StyleSheet`.
4. Manual visual check (optional): Launch app, navigate to Extended Output with a method that produces temperature_curve (e.g., French Press accurate mode) — chart renders immediately, no toggle button visible.
</verification>

<success_criteria>
- [ ] TempCurveInline.tsx toggle removed (no useState, no TouchableOpacity, no "View curve"/"Hide curve" text)
- [ ] Chart renders unconditionally inside the component
- [ ] Parent guard in extended.tsx unchanged (still gates on temperature_curve presence)
- [ ] TypeScript type-check passes
- [ ] No other files modified
</success_criteria>

<output>
After completion, create `.planning/quick/260405-o3p-remove-temperature-curve-toggle-on-exten/260405-o3p-SUMMARY.md`
</output>
